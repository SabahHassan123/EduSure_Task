import { Link } from "react-router-dom";

const Admin = () => {
  return (
    <div className="h-screen bg-gradient-to-r from-indigo-500 to-purple-600 flex flex-col items-center justify-center text-white text-center ">
      <h1 className="text-5xl font-bold mb-4">Admin</h1>
    </div>
  );
};

export default Admin;
